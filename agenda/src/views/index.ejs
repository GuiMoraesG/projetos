<%- include('includes/head') %>

    <div class="container">
        <main>

            <h1>
                <% if(typeof titulo !=='undefined' ) { %>
                    <%= titulo %>
                        <% } else { %>
                            o titulo não foi encontrado
                            <% } %>
            </h1>

            <form action="/" method="post">
                <label>Cliente</label>
                <input type="text" name="cliente">
                <button>Submit</button>
            </form>

        </main>

    </div>

<%- include('includes/footer') %>